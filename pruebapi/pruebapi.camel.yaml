- route:
    id: route-baef
    nodePrefixId: route-44d
    from:
      id: from-1347
      uri: platform-http
      parameters:
        path: /api
      steps:
        - log:
            id: log-f625
            message: ${body}
